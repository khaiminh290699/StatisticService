const getTotal = require("./statistic.getTotal");
const getInTime = require("./statistic.getInTime");
const topClickForum = require("./statistic.top.click.forum");
const topPostForum = require("./statistic.top.post.forum");

module.exports = {
  getInTime,
  getTotal,
  topClickForum,
  topPostForum
}